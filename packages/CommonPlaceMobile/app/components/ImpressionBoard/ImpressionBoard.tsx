import React from 'react';
import {Text, View} from 'react-native';

import {ImpressionBoardProps} from './ImpressionBoard.d';
import styles from './ImpressionBoard.scss';

const ImpressionBoard = ({}: ImpressionBoardProps) => {
  return (
    <View>
      <Text>ImpressionBoard</Text>
    </View>
  );
};

export default ImpressionBoard;
