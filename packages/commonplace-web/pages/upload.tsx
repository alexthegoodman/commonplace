import type { NextPage } from "next";
import PrimaryHeader from "../components/PrimaryHeader/PrimaryHeader";

const Upload: NextPage = () => {
  return (
    <section className="upload">
      <div className="uploadInner">Upload</div>
    </section>
  );
};

export default Upload;
