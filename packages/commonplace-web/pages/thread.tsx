import type { NextPage } from "next";
import PrimaryHeader from "../components/PrimaryHeader/PrimaryHeader";

const Thread: NextPage = () => {
  return (
    <section className="thread">
      <div className="threadInner">Thread</div>
    </section>
  );
};

export default Thread;
